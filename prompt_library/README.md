# Prompt Library

This folder contains the curated version of your imported prompt collection.

Each prompt concept gets its own isolated folder with:
- the copied prompt file or files
- a `README.md` explaining why the folder exists
- source provenance back to the original location on disk

Use this folder when you want meaning-first navigation instead of the raw original paths.
